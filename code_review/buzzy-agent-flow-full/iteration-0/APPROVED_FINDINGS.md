# Approved Findings

Check only the verified findings a human wants the fixer to address in this PR.
The fixer must ignore unchecked items.

- [ ] `RF-001` `high` components/Navigation.tsx:63 - New `/buzzy-agent` links can ship without a tracked route (triage: fix-now)
